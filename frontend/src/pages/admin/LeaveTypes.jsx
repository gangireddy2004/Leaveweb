import { useState } from 'react'
import { Button, Card, PageHeader } from '../../components/UI'
import { leaveTypes } from '../../data/mockData'
export default function LeaveTypes() { const [types, setTypes] = useState(leaveTypes.map((t) => ({ ...t, enabled: true }))); return <><PageHeader eyebrow="Admin / Policy" title="Leave types" description="Shape the leave policy your team works with." action={<Button>+ Add leave type</Button>} /><div className="type-grid">{types.map((type) => <Card className="type-card" key={type.id}><div className={`type-accent ${type.color}`}></div><div className="type-card-top"><span className="eyebrow">{type.id.toUpperCase()}</span><button className={type.enabled ? 'toggle on' : 'toggle'} onClick={() => setTypes(types.map((item) => item.id === type.id ? { ...item, enabled: !item.enabled } : item))}><i></i></button></div><h3>{type.name}</h3><p>{type.description}</p><div className="type-card-foot"><strong>{type.total} <small>days / year</small></strong><button className="table-link">Edit</button></div></Card>)}</div></> }