import { useState } from 'react'
import { Link } from 'react-router-dom'
import { Card, PageHeader, SelectField, StatusBadge } from '../components/UI'
import { mockLeaves } from '../data/mockData'
export default function MyLeaves() { const [query, setQuery] = useState(''); const [status, setStatus] = useState('All statuses'); const filtered = mockLeaves.filter((item) => `${item.type} ${item.id} ${item.reason}`.toLowerCase().includes(query.toLowerCase()) && (status === 'All statuses' || item.status === status)); return <><PageHeader eyebrow="Your history" title="My leaves" description="Every request, all in one place." action={<Link className="button button-primary" to="/apply-leave">+ New request</Link>} /><Card className="table-card"><div className="toolbar"><label className="search-field">⌕<input placeholder="Search requests" value={query} onChange={(e) => setQuery(e.target.value)} /></label><SelectField label=""><option>All leave types</option><option>Casual leave</option><option>Sick leave</option><option>Earned leave</option></SelectField><SelectField label="" value={status} onChange={(e) => setStatus(e.target.value)}><option>All statuses</option><option>Pending</option><option>Approved</option><option>Rejected</option><option>Cancelled</option></SelectField></div><div className="table-wrap"><table><thead><tr><th>Request</th><th>Dates</th><th>Days</th><th>Reason</th><th>Status</th><th>Applied</th><th></th></tr></thead><tbody>{filtered.map((leave) => <tr key={leave.id}><td><strong>{leave.type}</strong><small>{leave.id}</small></td><td>{leave.start}<br /><span className="muted">to {leave.end}</span></td><td>{leave.days}</td><td className="reason-cell">{leave.reason}</td><td><StatusBadge status={leave.status} /></td><td>{leave.applied}</td><td><Link className="table-link" to={`/leaves/${leave.id}`}>View</Link></td></tr>)}</tbody></table>{!filtered.length && <div className="empty-state"><strong>No matching requests</strong><p>Try changing your search or filters.</p></div>}</div></Card></> }